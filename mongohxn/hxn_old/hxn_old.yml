metadatastore:
    module: databroker.headersource.mongo
    class: MDS
    config:
        host: 'gpu-001'
        port: 27019
        database: 'datastore'
        timezone: 'US/Eastern'
assets:
    module: databroker.assets.mongo
    class: Registry
    config:
        host: 'gpu-001'
        port: 27019
        database: 'filestore'

